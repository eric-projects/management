import { Component } from 'vue-property-decorator';
import { BaseConfig } from '../base-config';

@Component
export class MmtWangeditorConfig extends BaseConfig {
    created(): void {
    }
    render() {
        return '';
    }
}
