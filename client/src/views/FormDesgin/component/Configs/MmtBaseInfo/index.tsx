import { Component, Prop, Vue } from 'vue-property-decorator';
import { BaseConfig } from '../base-config';

@Component({
})
export class MmtBaseInfoConfig extends BaseConfig {
  created(): void {
  }
  render() {
    return (
      <div>
      </div>
    );
  }
}
